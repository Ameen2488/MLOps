# Exercise: Feast Integration

**Objective**: Initialize a Feast repository.

## 📝 Steps

1. Install Feast: `pip install feast`.
2. Run `feast init my_feature_repo`.
3. Explore the created files: `feature_store.yaml`, `example.py`.
4. What is the `OnlineStore` set to by default? (Hint: Check yaml).

```bash
# Terminal Commands
pip install feast
feast init feature_repo
cd feature_repo
cat feature_store.yaml
```
