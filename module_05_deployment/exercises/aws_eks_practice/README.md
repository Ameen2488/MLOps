# Exercise: AWS EKS Practice

**Objective**: Understand EKS concepts.

## 📝 Steps

1. What tool is commonly used to create EKS clusters easily? (Answer: `eksctl`).
2. Write the command to create a cluster named `demo-cluster` in `us-east-1` with 2 nodes.

> Answer: `eksctl create cluster --name demo-cluster --region us-east-1 --nodes 2`
